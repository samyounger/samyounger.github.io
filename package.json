{
  "private": true,
  "scripts": {
    "build:search": "rm -rf _site/pagefind && pagefind --site _site",
    "serve:search": "JEKYLL_ENV=production bundle exec jekyll build && npm run build:search && JEKYLL_ENV=production bundle exec jekyll serve --skip-initial-build --host 127.0.0.1 --port 4000"
  },
  "devDependencies": {
    "pagefind": "^1.4.0"
  }
}
